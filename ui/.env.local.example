# Local dev only — browser calls FastAPI directly (skip /api proxy):
NEXT_PUBLIC_API_URL=http://localhost:8000

# Vercel production: set API_URL only (runtime; powers /api proxy to Railway).
# Do not set NEXT_PUBLIC_API_URL in production.
API_URL=https://your-api.up.railway.app
